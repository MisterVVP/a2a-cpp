name: Apply PR 147 initializer lint fix

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.number == 147
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: "dev-fix-unresolved-review-threads-in-pr-#145"
          fetch-depth: 0
      - name: Fix aggregate initializer
